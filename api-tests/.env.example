# Copie para .env (gitignored) e preencha com um usuário de stage.
SITTAX_LOGIN=usuario@exemplo.com.br
SITTAX_PASSWORD=sua-senha
# Opcional — sobrescreve a base da API:
# BASE_URL=https://api.certificados.stage.sittax.com.br/v1
